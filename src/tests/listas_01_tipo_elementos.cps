// Test: Tipo correcto de elementos en listas
function main() {
    let numeros: integer[] = [1, 2, 3, 4, 5];      // Elementos integer ✓
    let textos: string[] = ["a", "b", "c"];         // Elementos string ✓
    let banderas: boolean[] = [true, false, true];  // Elementos boolean ✓
    
    print(numeros[0]);
}
