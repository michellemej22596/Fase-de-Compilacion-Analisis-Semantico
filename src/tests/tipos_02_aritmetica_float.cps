// Test: Operaciones aritméticas con float (+, -, *, /)
function main() {
    let x: float = 3.14;
    let y: float = 2.0;
    
    let suma: float = x + y;        // 5.14
    let resta: float = x - y;       // 1.14
    let mult: float = x * y;        // 6.28
    let div: float = x / y;         // 1.57
    
    print(suma);
}
