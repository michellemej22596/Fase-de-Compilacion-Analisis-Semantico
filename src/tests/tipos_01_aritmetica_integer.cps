// Test: Operaciones aritméticas con integer (+, -, *, /)
function main() {
    let a: integer = 10;
    let b: integer = 5;
    
    let suma: integer = a + b;      // 15
    let resta: integer = a - b;     // 5  
    let mult: integer = a * b;      // 50
    let div: integer = a / b;       // 2
    
    print(suma);
}
