// Demuestra: declaración de variables, tipos primitivos, constantes

let nombre: string = "Ana";
var edad: integer = 25;
const PI: float = 3.14159;
let activo: boolean = true;

function main(): void {
    print("Nombre: " + nombre);
    print("Edad: " + edad);
    print("PI: " + PI);
    print("Activo: " + activo);
}
